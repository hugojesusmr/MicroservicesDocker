
server:
  port: 8761

eureka:
  instance:
    hostname: 172.21.0.1
  client:
    registerWithEureka: false
    fetchRegistry: false
    serviceUrl:
      default-zone: http://${eureka.instance.hostname}:${server.port}/eureka/
      
